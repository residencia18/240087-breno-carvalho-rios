import { WikipediaSpanToStrongPipe } from './wikipedia-span-to-strong.pipe';

describe('WikipediaSpanToStrongPipe', () => {
  it('create an instance', () => {
    const pipe = new WikipediaSpanToStrongPipe();
    expect(pipe).toBeTruthy();
  });
});
