import { UnderlinedDirective } from './underlined.directive';

describe('UnderlinedDirective', () => {
  it('should create an instance', () => {
    const directive = new UnderlinedDirective();
    expect(directive).toBeTruthy();
  });
});
