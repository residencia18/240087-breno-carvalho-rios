import { BeStrongDirective } from './be-strong.directive';

describe('BeStrongDirective', () => {
  it('should create an instance', () => {
    const directive = new BeStrongDirective();
    expect(directive).toBeTruthy();
  });
});
